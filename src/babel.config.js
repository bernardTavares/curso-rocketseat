module.exports = {
    // presets sao conjuntos de configuração criadas por terceiros que podemos reaproveitar na nossa aplicação
    presets: [
        '@babel/preset-env',
        '@babel/preset-react'
    ],
}